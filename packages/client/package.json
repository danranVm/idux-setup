{
  "name": "@idux/setup-admin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist node_modules",
    "dev": "vite",
    "build": "run-p type-check build-only",
    "build-only": "vite build",
    "type-check": "vue-tsc --noEmit -p tsconfig.vitest.json --composite false"
  },
  "dependencies": {
    "@idux/setup-shared": "workspace:*",
    "@idux/setup-theme": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
